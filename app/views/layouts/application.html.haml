!!!
%html{lang: "en"}
  %head
    %meta{charset: "UTF-8"}
    %meta{name: "viewport", content: "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : "Burning Man Seven Pools Chat"
    
    / Prevent FOUC
    :css
      body { opacity: 0; }
      
    = csrf_meta_tags if defined?(csrf_meta_tags)
    
    / System fonts for fastest loading
    :css
      body {
        margin: 0;
        padding: 0;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        font-size: 16px;
        line-height: 1.5;
        color: #333;
        background: #fafafa;
        opacity: 1;
        transition: opacity 0.3s ease;
      }
      
      * {
        box-sizing: border-box;
      }
      
  %body
    = yield