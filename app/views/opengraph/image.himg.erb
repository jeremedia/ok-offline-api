<!DOCTYPE html>
<html>
<head>
  <style>
    @font-face {
      font-family: 'Berkeley Mono';
      font-style: normal;
      font-weight: 400;
      src: url('<%= "#{base_url}/fonts/BerkeleyMono-Regular.woff2" %>') format('woff2'),
           url('<%= "#{base_url}/fonts/BerkeleyMono-Regular.woff" %>') format('woff');
    }
    
    @font-face {
      font-family: 'Berkeley Mono';
      font-style: normal;
      font-weight: 700;
      src: url('<%= "#{base_url}/fonts/BerkeleyMono-Bold.woff2" %>') format('woff2'),
           url('<%= "#{base_url}/fonts/BerkeleyMono-Bold.woff" %>') format('woff');
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      width: 1200px;
      height: 630px;
      background: #000000;
      color: #FFFFFF;
      font-family: 'Berkeley Mono', monospace;
      display: flex;
      align-items: center;
      padding: 80px;
      position: relative;
      overflow: hidden;
    }
    
    .content {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      z-index: 2;
      max-width: 700px;
    }
    
    .logo {
      position: absolute;
      right: 80px;
      top: 50%;
      transform: translateY(-50%);
      width: 300px;
      height: 300px;
      opacity: 0.9;
    }
    
    .title {
      font-size: 72px;
      font-weight: 700;
      line-height: 1.1;
      margin-bottom: 24px;
      letter-spacing: -0.02em;
    }
    
    .subtitle {
      font-size: 32px;
      font-weight: 400;
      line-height: 1.3;
      opacity: 0.8;
      margin-bottom: 40px;
    }
    
    .meta {
      font-size: 24px;
      opacity: 0.6;
      display: flex;
      align-items: center;
      gap: 20px;
    }
    
    .divider {
      width: 2px;
      height: 24px;
      background: #FFFFFF;
      opacity: 0.3;
    }
    
    .gradient-overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: radial-gradient(ellipse at center right, transparent 30%, rgba(0,0,0,0.4) 70%);
      z-index: 1;
    }
  </style>
</head>
<body>
  <div class="gradient-overlay"></div>
  
  <div class="content">
    <h1 class="title"><%= title %></h1>
    <% if subtitle.present? %>
      <p class="subtitle"><%= subtitle %></p>
    <% end %>
    <div class="meta">
      <span>OKNOTOK</span>
      <div class="divider"></div>
      <span>BRC <%= year %></span>
    </div>
  </div>
  
  <img class="logo" src="<%= "#{base_url}/ok_logos/oknotok_circle_mark.png" %>" alt="OKNOTOK">
</body>
</html>